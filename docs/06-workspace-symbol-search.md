# Workspace Symbol Search

Workspace symbol provider enables global search across the project for:

- Classes, methods, properties, functions, and constants (best-effort)

Backed by a workspace index built from scanning PHP files.

Entry point: `extension.js:330`.


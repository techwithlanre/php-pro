# Semantic Highlighting

Document semantic tokens for `php` provide semantic coloring for:

- Classes / interfaces / enums
- Functions
- Variables
- Constants

Also highlights DQL keywords and parameters inside `createQuery("...")` strings.

Entry point: semantic tokens provider in `extension.js:2372`.


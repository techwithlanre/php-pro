# Run PHP File

Command:

- `PHP: Run Current File` (`php.runFile`)

Runs the active PHP file using the configured PHP executable (`php.executablePath`) in an integrated terminal.

Entry point: command registration in `extension.js` (`php.runFile`).


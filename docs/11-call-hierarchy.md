# Call Hierarchy

Call hierarchy for `php` shows:

- Incoming calls (who calls this)
- Outgoing calls (what this calls)

This is a best-effort implementation based on parsing and workspace indexing.

Entry point: `extension.js:374`.


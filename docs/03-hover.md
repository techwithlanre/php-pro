# Hover Information

Hover provider for `php` shows:

- Inferred variable type for simple cases (assignment-based inference)
- Function/method signature when known from workspace parsing
- Built-in PHP function category (fallback)

Entry point: hover provider in `extension.js:259`.

